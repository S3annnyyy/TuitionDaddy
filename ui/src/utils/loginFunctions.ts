export const handleSubmit = (email: string, password: string): boolean => {
  console.log('Email:', email);
  console.log('Password:', password);
  // Add in logic for authentication and DB connection
  return true 
};